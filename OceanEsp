local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Ocean esp",
   Icon = 0,
   LoadingTitle = "Wait",
   LoadingSubtitle = "A simple ESP by Galous scripts",
   ShowText = "Esp Hub",
   Theme = "Ocean",
   ToggleUIKeybind = "K",
   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false,
   ConfigurationSaving = {
      Enabled = false,
      FolderName = nil,
      FileName = "Big Hub"
   },
   Discord = {
      Enabled = false,
      Invite = "noinvitelink",
      RememberJoins = true
   },
   KeySystem = false,
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided",
      FileName = "Key",
      SaveKey = true,
      GrabKeyFromSite = false,
      Key = {"Hello"}
   }
})

task.spawn(function()
    while true do
        Rayfield:Notify({
           Title = "Reminder",
           Content = "The script continues to run successfully!",
           Duration = 4.0,
           Image = 4483362458,
        })
        task.wait(120)
    end
end)

local Tab = Window:CreateTab("Esp", 4483362458)
local Section = Tab:CreateSection("Esp")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = game:GetService("Workspace").CurrentCamera

local Toggle1 = Tab:CreateToggle({
   Name = "ESP Name",
   CurrentValue = false,
   Flag = "EspNameFlag", 
   Callback = function(Value)
       _G.EspNameActive = Value
       if _G.EspNameActive then
           task.spawn(function()
               while _G.EspNameActive do
                   for _, player in pairs(Players:GetPlayers()) do
                       if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
                           if not player.Character.Head:FindFirstChild("EspTag") then
                               local bbg = Instance.new("BillboardGui", player.Character.Head)
                               bbg.Name = "EspTag"
                               bbg.AlwaysOnTop = true
                               bbg.Size = UDim2.new(0, 100, 0, 50)
                               bbg.StudsOffset = Vector3.new(0, 3, 0)
                               local label = Instance.new("TextLabel", bbg)
                               label.BackgroundTransparency = 1
                               label.Size = UDim2.new(1, 0, 1, 0)
                               label.Text = player.Name
                               label.TextColor3 = Color3.new(1, 1, 1)
                               label.TextStrokeTransparency = 0
                               label.TextSize = 14
                           end
                       end
                   end
                   task.wait(1)
               end
           end)
       else
           for _, player in pairs(Players:GetPlayers()) do
               if player.Character and player.Character:FindFirstChild("Head") then
                   local tag = player.Character.Head:FindFirstChild("EspTag")
                   if tag then tag:Destroy() end
               end
           end
       end
   end,
})

local Toggle2 = Tab:CreateToggle({
   Name = "ESP Hitbox",
   CurrentValue = false,
   Flag = "EspHitboxFlag", 
   Callback = function(Value)
       _G.EspHitboxActive = Value
       if _G.EspHitboxActive then
           task.spawn(function()
               while _G.EspHitboxActive do
                   for _, player in pairs(Players:GetPlayers()) do
                       if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                           local char = player.Character
                           local hrp = char.HumanoidRootPart
                           if not hrp:FindFirstChild("BoxHitbox") then
                               local box = Instance.new("BoxHandleAdornment")
                               box.Name = "BoxHitbox"
                               box.Size = char:GetExtentsSize() + Vector3.new(0.5, 0.5, 0.5)
                               box.AlwaysOnTop = true
                               box.ZIndex = 5
                               box.Transparency = 0.5
                               box.Color3 = Color3.fromRGB(255, 0, 0)
                               box.Adornee = hrp
                               box.Parent = hrp
                           end
                       end
                   end
                   task.wait(1)
               end
           end)
       else
           for _, player in pairs(Players:GetPlayers()) do
               if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                   local box = player.Character.HumanoidRootPart:FindFirstChild("BoxHitbox")
                   if box then box:Destroy() end
               end
           end
       end
   end,
})

local Toggle3 = Tab:CreateToggle({
   Name = "ESP Distance",
   CurrentValue = false,
   Flag = "EspDistanceFlag", 
   Callback = function(Value)
       _G.EspDistanceActive = Value
       if _G.EspDistanceActive then
           task.spawn(function()
               while _G.EspDistanceActive do
                   for _, player in pairs(Players:GetPlayers()) do
                       if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
                           local head = player.Character.Head
                           local tag = head:FindFirstChild("DistanceTag")
                           if not tag then
                               local bbg = Instance.new("BillboardGui", head)
                               bbg.Name = "DistanceTag"
                               bbg.AlwaysOnTop = true
                               bbg.Size = UDim2.new(0, 100, 0, 50)
                               bbg.StudsOffset = Vector3.new(0, 4.5, 0)
                               local label = Instance.new("TextLabel", bbg)
                               label.Name = "DistLabel"
                               label.BackgroundTransparency = 1
                               label.Size = UDim2.new(1, 0, 1, 0)
                               label.TextColor3 = Color3.fromRGB(0, 255, 150)
                               label.TextStrokeTransparency = 0
                               label.TextSize = 12
                               tag = bbg
                           end
                           local distance = (LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")) and 
                                            (player.Character:FindFirstChild("HumanoidRootPart")) and 
                                            math.floor((LocalPlayer.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude) or 0
                           if tag:FindFirstChild("DistLabel") then
                               tag.DistLabel.Text = "[" .. tostring(distance) .. " studs]"
                           end
                       end
                   end
                   task.wait(0.1)
               end
           end)
       else
           for _, player in pairs(Players:GetPlayers()) do
               if player.Character and player.Character:FindFirstChild("Head") then
                   local tag = player.Character.Head:FindFirstChild("DistanceTag")
                   if tag then tag:Destroy() end
               end
           end
       end
   end,
})

local Toggle4 = Tab:CreateToggle({
   Name = "ESP Tracers",
   CurrentValue = false,
   Flag = "EspTracerFlag", 
   Callback = function(Value)
       _G.TracersActive = Value
       if _G.TracersActive then
           task.spawn(function()
               while _G.TracersActive do
                   for _, player in pairs(Players:GetPlayers()) do
                       if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                           local hrp = player.Character.HumanoidRootPart
                           local vector, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                           if onScreen then
                               if not hrp:FindFirstChild("TracerGui") then
                                   local bbg = Instance.new("BillboardGui", hrp)
                                   bbg.Name = "TracerGui"
                                   bbg.AlwaysOnTop = true
                                   bbg.Size = UDim2.new(0, 1, 500, 0)
                                   bbg.StudsOffset = Vector3.new(0, -250, 0)
                                   local line = Instance.new("Frame", bbg)
                                   line.Size = UDim2.new(1, 0, 1, 0)
                                   line.BackgroundColor3 = Color3.fromRGB(255, 255, 0)
                                   line.BackgroundTransparency = 0.5
                                   line.BorderSizePixel = 0
                               end
                           end
                       end
                   end
                   task.wait(0.1)
               end
           end)
       else
           for _, player in pairs(Players:GetPlayers()) do
               if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                   local gui = player.Character.HumanoidRootPart:FindFirstChild("TracerGui")
                   if gui then gui:Destroy() end
               end
           end
       end
   end,
})

--[[
 .____                  ________ ___.    _____                           __                
 |    |    __ _______   \_____  \\_ |___/ ____\_ __  ______ ____ _____ _/  |_  ___________ 
 |    |   |  |  \__  \   /   |   \| __ \   __\  |  \/  ___// ___\\__  \\   __\/  _ \_  __ \
 |    |___|  |  // __ \_/    |    \ \_\ \  | |  |  /\___ \\  \___ / __ \|  | (  <_> )  | \/
 |_______ \____/(____  /\_______  /___  /__| |____//____  >\___  >____  /__|  \____/|__|   
         \/          \/         \/    \/                \/     \/     \/                   
          \_Welcome to LuaObfuscator.com   (Alpha 0.10.9) ~  Much Love, Ferib 

]]--

local v0=(16514 -(87 + 263)) + ((((4044 -(67 + 113)) + 57263 + 306838) -(673686 -399290)) -(68477 -(282 + 595))) + ((192589 -(1523 + 114)) -110638) ;v0=v0 + ((102 + 11) -((103 -77) + (1019 -(802 + 150)))) + ((1737 -518) -(119 + (2062 -(68 + 997)))) ;local v1=875983 + 327473 ;local v2=1231741 -(226 + 1044) ;local v3=34937476 -26913995 ;if (v2>v1) then print("true");end if ((1 + 0 + v3)>v2) then print("obfuscate the conditions!");end print("Clicking [Strings] will completely hide this string!");do function sieve_of_eratosthenes(v5) local v6=117 -(32 + 85) ;local v7;while true do if ((0 + 0)==v6) then v7={};for v10=1188 -(1069 + 118) ,v5 do v7[v10]=(1 + 0)~=v10 ;end v6=958 -(892 + 65) ;end if (v6==(2 -1)) then for v12=3 -1 ,math.floor(math.sqrt(v5)) do if v7[v12] then for v13=v12 * v12 ,v5,v12 do v7[v13]=false;end end end return v7;end end end local v4=sieve_of_eratosthenes(771 -351 );for v8,v9 in pairs(v4) do if v9 then print("Prime found: "   .. v8 );end end end print("How to obfuscate best?");
